# Extract the data frame or sample frame from a panel

Extract the data frame or sample frame from a panel

## Usage

``` r
panel_data(x)

sample_frame(x)
```

## Arguments

- x:

  A `panel`.

## Value

`panel_data()` returns the data frame; `sample_frame()` returns the
`sfw` (with its rules and provenance log).

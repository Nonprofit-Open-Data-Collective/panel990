% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-complete.R
\name{panel_complete}
\alias{panel_complete}
\title{Complete panel spans by filling interior gaps}
\usage{
panel_complete(
  data,
  method = c("interpolate", "mean", "locf", "nocb"),
  types = c("persistent", "entrant", "exit", "transient"),
  max_gap_size = Inf,
  max_gap_count = Inf,
  vars = NULL,
  time = "TAX_YEAR",
  id = "EIN2",
  as_integers = FALSE,
  classification = NULL,
  reconcile = FALSE,
  reconcile_fixed = NULL,
  reconcile_section = NULL
)
}
\arguments{
\item{data}{A panel data frame.}

\item{method}{Fill method: \code{"interpolate"} (linear, the default), \code{"mean"},
\code{"locf"} (carry forward), or \code{"nocb"} (carry backward).}

\item{types}{Panel types eligible for completion. Defaults to every span
type; only \code{segmented} organizations have interior years to fill.}

\item{max_gap_size, max_gap_count}{Optional gap-size / gap-count limits per
organization.}

\item{vars}{Numeric variables to fill; \code{NULL} selects numeric non-key fields.}

\item{time}{Panel-time column.}

\item{id}{Panel-ID column.}

\item{as_integers}{Round filled values for originally integer fields.}

\item{classification}{Optional classification from \code{\link[=panel_describe]{panel_describe()}}.}

\item{reconcile}{If \code{TRUE}, snap the newly filled rows to the accounting
identities with \code{\link[=reconcile]{reconcile()}} after imputation. Filling a whole row
preserves linear identities, but rounding (\code{as_integers}) or an inconsistent
bracketing observation can leave an imputed row off-balance; this corrects
it with the least change.}

\item{reconcile_fixed, reconcile_section}{Passed to \code{\link[=reconcile]{reconcile()}} when
\code{reconcile = TRUE} (columns to hold fixed; identity sections to enforce).}
}
\value{
The panel with interior gaps filled; inserted rows carry
\code{imputed_row = TRUE}.
}
\description{
Turns every \code{segmented} organization into a \code{seamless} one by inserting the
missing interior years and filling their numeric fields, so each
organization is observed continuously across its own span. This is the
researcher-facing wrapper over \code{\link[=panel_impute]{panel_impute()}}: it fills gaps for \strong{all}
boundary types (not just panel-spanning organizations) and interpolates by
default.
}
\details{
Completion never extends past an organization's observed span -- it fills the
holes, it does not extrapolate to the panel edges. Inserted rows are flagged
with \code{imputed_row}.
}
\seealso{
\code{\link[=panel_impute]{panel_impute()}}, \code{\link[=panel_describe]{panel_describe()}}, \code{\link[=reconcile]{reconcile()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download_tables}
\alias{download_tables}
\title{Download or reuse efile table CSV files}
\usage{
download_tables(
  years,
  tables,
  source = data_source(),
  path = "PANEL990",
  cache = c("retain", "temporary"),
  overwrite = FALSE,
  retry_max = 3L,
  timeout = 1800,
  verbose = TRUE
)
}
\arguments{
\item{years}{Integer tax years.}

\item{tables}{Aliases or literal canonical table names.}

\item{source}{An \code{\link[=data_source]{data_source()}} configuration.}

\item{path}{Cache directory used when \code{cache = "retain"}.}

\item{cache}{\code{"retain"} for a durable cache or \code{"temporary"} for a session
temporary directory.}

\item{overwrite}{Download again even when a cached file exists.}

\item{retry_max}{Maximum attempts per remote file.}

\item{timeout}{Minimum per-attempt download timeout in seconds; raised
automatically for large files. See the Timeouts section.}

\item{verbose}{Print per-file progress messages.}
}
\value{
An \code{download_result} containing successful file paths, a structured
table-year manifest, the \code{run_id}, and the per-run \code{log_file}.
}
\description{
Each file is fetched by \code{\link[=.p990_fetch]{.p990_fetch()}}, which raises the download timeout,
retries with exponential backoff, and discards partial files between
attempts. Progress is reported as a START line before each transfer begins
and an OK/FAIL line when it settles, so a stalled file is identifiable while
it is still running.
}
\details{
Every call writes its own log under \verb{<path>/logs} named for the run, so
repeated runs accumulate rather than overwrite. See \code{\link[=retrieval_log]{retrieval_log()}}.
}
\section{Timeouts}{

R's download timeout is a budget for an entire transfer, not a limit on how
long the connection may stall, so a fixed value silently imposes a minimum
transfer rate: the 2023 header table alone is 357 MB. \code{timeout} is therefore
treated as a floor. Each file is given at least \code{timeout} seconds and at
least as long as its \code{Content-Length} needs at 0.5 MB/s, and a timeout the
user raised globally with \code{options(timeout =)} or
\code{R_DEFAULT_INTERNET_TIMEOUT} is never lowered.
}


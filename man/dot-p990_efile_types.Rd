% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format.R
\name{.p990_efile_types}
\alias{.p990_efile_types}
\title{Declared efile type of each field}
\usage{
.p990_efile_types(fields = NULL)
}
\arguments{
\item{fields}{Optional character vector to restrict the result to.}
}
\value{
A named character vector of \code{variable_name} -> efile type, one of
\code{"numeric"}, \code{"integer"}, \code{"checkbox"}, \code{"text"}, or \code{"date"}.
}
\description{
Parquet stores every efile column as a string, so a read needs an external
statement of which fields are numeric. This is that statement: the 16
structural filing columns from \code{.EFILE_KEY_TYPES}, and every form variable
from \code{field_concordance$data_type_simple}.
}
\details{
Using the concordance makes the parquet types \emph{declared} rather than
inferred, which is the one respect in which the parquet path is better than
the CSV path: \code{read_csv_auto()} and \code{fread()} guess from the first rows and
disagree with each other on real tables.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accounting.R
\name{reconcile}
\alias{reconcile}
\title{Reconcile 990 rows to accounting identities with the least change}
\usage{
reconcile(
  data,
  section = "revenue",
  fixed = NULL,
  weights = NULL,
  rows = NULL,
  id = "EIN2",
  time = "TAX_YEAR"
)
}
\arguments{
\item{data}{A data frame of 990 financial fields.}

\item{section}{Identity sections to enforce. Default \code{"revenue"}.}

\item{fixed}{Character vector of columns to hold fixed (e.g. reported totals).}

\item{weights}{Optional named vector of per-variable weights (default equal).}

\item{rows}{Rows to reconcile: a logical/integer index. Default is rows where
\code{imputed_row} is \code{TRUE} if that column exists, otherwise all rows.}

\item{id, time}{Identifier columns (unused by the math; kept for symmetry).}
}
\value{
\code{data} with reconciled values; an \code{"reconciled"} attribute records how
many rows were adjusted and how many were skipped for missing values.
}
\description{
Adjusts values as little as possible (weighted least squares) so that each
row satisfies the bundled \link{accounting_identities}. Columns named in \code{fixed}
are held exactly; among the rest, \code{weights} set how resistant each variable is
to change (larger = moves less). Only rows with no missing values in the
relevant fields are reconciled.
}
\seealso{
\code{\link[=accounting_check]{accounting_check()}}, \code{\link[=panel_complete]{panel_complete()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample-frame.R
\name{add_rule}
\alias{add_rule}
\title{Add or replace a rule on a sample frame}
\usage{
add_rule(sfw, name = NULL, type, ...)
}
\arguments{
\item{sfw}{A sample frame.}

\item{name}{Rule name. Empty/\code{NULL} auto-generates \verb{<type>_<n>}. An existing
name replaces that rule.}

\item{type}{One of the rule types above.}

\item{...}{Type-specific payload (see Details).}
}
\value{
The updated sample frame.
}
\description{
Appends a typed rule, or replaces the rule with the same \code{name} (upsert). The
payload arguments depend on \code{type}:
\describe{
\item{\code{filter}, \code{check}}{\code{column}, \code{op}, \code{values} (structured) \strong{or}
\code{expr} (a predicate string). \code{op} in \verb{in}, \code{not_in}, \code{==}, \code{!=}, \code{>},
\code{>=}, \code{<}, \code{<=}, \code{between}, \code{is_true}, \code{is_false}.}
\item{\code{subset}}{\code{subset} -- a vector of entity ids to keep (captured).}
\item{\code{label}}{\code{map} (an id-named vector) or \code{from} (a data frame) with
\code{keys} and \code{label} column names; \code{label} also names the derived column.}
\item{\code{select}}{\code{vars}, \code{scope}, \code{tables}, \code{drop} (resolved via
\link{field_concordance}).}
\item{\code{dedup}}{\code{group}, \code{partial}, \code{amended}, \code{timestamp} column overrides
for \code{\link[=deduplicate]{deduplicate()}}.}
\item{\code{refresh}}{\code{fn} -- a function taking and returning a data frame.}
\item{\code{view}}{\code{rows}, \code{cols}, \code{value}, \code{fun} -- a crosstab/tapply summary.}
\item{\code{function}}{\code{value}/\code{code} (a string) or \code{fn} (a function).}
}
}
\seealso{
\code{\link[=update_rule]{update_rule()}}, \code{\link[=get_rules]{get_rules()}}, \code{\link[=apply_sfw]{apply_sfw()}}.
}

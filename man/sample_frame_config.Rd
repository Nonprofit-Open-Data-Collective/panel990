% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample-frame.R
\name{sample_frame_config}
\alias{sample_frame_config}
\alias{set_keys}
\alias{set_meta}
\alias{keep_cols}
\alias{add_cohort}
\alias{set_dedup}
\alias{set_policy}
\title{Configure a sample frame's keys, columns, cohorts, and policies}
\usage{
set_keys(sfw, id = NULL, time = NULL)

set_meta(sfw, ...)

keep_cols(sfw, vars = NULL, scope = NULL, tables = NULL, drop = NULL)

add_cohort(sfw, name, ids)

set_dedup(sfw, rule)

set_policy(sfw, ...)
}
\arguments{
\item{sfw}{A \code{sample_frame}.}

\item{id, time}{Key column names (\code{set_keys}).}

\item{...}{Named metadata (\code{set_meta}) or named policies (\code{set_policy}).}

\item{vars}{Explicit variable names to keep (additive; \code{keep_cols}).}

\item{scope}{Column scope: form codes \code{PC}/\code{EZ}/\code{PZ}/\code{HD}/\code{SG}, or a form
presence value \code{"both"}/\code{"990"}/\code{"990EZ"}/\code{"all"} (resolved via
\code{\link[=fields_in_scope]{fields_in_scope()}}). Header (\code{HD}) fields are always kept.}

\item{tables}{Table specs to keep columns from: bare part tokens (\code{"P01"}) or
\code{rdb_table} prefixes.}

\item{drop}{Columns to drop (never drops header/key columns).}

\item{name}{Cohort name (\code{add_cohort}).}

\item{ids}{Character vector of identifiers for the cohort (\code{add_cohort}).}

\item{rule}{Deduplication rule/preset passed to downstream steps (\code{set_dedup}).}
}
\value{
The updated \code{sample_frame}.
}
\description{
Configure a sample frame's keys, columns, cohorts, and policies
}

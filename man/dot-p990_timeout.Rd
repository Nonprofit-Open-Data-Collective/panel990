% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve.R
\name{.p990_timeout}
\alias{.p990_timeout}
\title{Timeout budget for one transfer attempt}
\usage{
.p990_timeout(timeout, bytes = NA_real_, floor = NULL)
}
\arguments{
\item{timeout}{Requested per-attempt timeout in seconds.}

\item{bytes}{Expected transfer size in bytes, or \code{NA}.}

\item{floor}{A timeout already in force that must not be reduced.}
}
\value{
A length-one integer number of seconds.
}
\description{
R's \code{timeout} option is a budget for the \emph{whole} transfer, not an idle
timeout: \code{\link[utils:download.file]{utils::download.file()}} aborts once it elapses even while bytes
are still arriving. A fixed value therefore imposes a minimum transfer rate
that rises with file size -- 3.6 GB in 1800 seconds demands a sustained
2 MB/s. Where the size is known, convert it into the time it needs at
\code{.P990_MIN_RATE} and keep whichever budget is larger.
}
\details{
\code{floor} preserves a timeout the user raised globally, through
\code{options(timeout =)} or the \code{R_DEFAULT_INTERNET_TIMEOUT} environment
variable; \code{?download.file} asks packages not to lower it.
}
\keyword{internal}

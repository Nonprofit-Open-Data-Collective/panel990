% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frame-resolve.R
\name{resolve_frame}
\alias{resolve_frame}
\title{Resolve a sample frame's cross-year requirements against the source}
\usage{
resolve_frame(
  sfw,
  years,
  source = data_source(),
  name = "resolved",
  verbose = TRUE
)
}
\arguments{
\item{sfw}{A sample frame carrying at least one \code{require} rule.}

\item{years}{Tax years the requirement is evaluated over. Years the release
does not carry are skipped, and \code{present_in = "all"} then means all of the
years that exist.}

\item{source}{An \code{\link[=data_source]{data_source()}} configuration.}

\item{name}{Name for the \code{subset} rule that receives the resolved ids.}

\item{verbose}{Print a line per resolved rule.}
}
\value{
The updated sample frame: \code{require} rules deactivated, one \code{subset}
rule added, and a receipt appended to the provenance log.
}
\description{
Executes every active \code{require} rule on the frame and replaces them with a
single captured \code{subset} rule, so the resulting frame specifies one concrete
set of entity ids that \code{\link[=panelize]{panelize()}} can push down to read-time and
\code{\link[=apply_sfw]{apply_sfw()}} can execute against any data frame.

A \code{require} rule is the one rule type that cannot be evaluated against a
data frame, because it asserts something across table-years -- "filed in all
three years", "was a 990EZ filer every year". See \code{\link[=add_rule]{add_rule()}} for the
payload.

Resolution reads only the entity key, the time key, and any column a
predicate names, so it is far cheaper than building the panel and filtering
it: the condition is answered before the financial columns are ever touched.
On a \code{data_source(format = "parquet")} release this is two columns of one
table.
}
\details{
Multiple \code{require} rules intersect: an entity must satisfy all of them. The
rules themselves are deactivated rather than dropped, so the frame still
records the condition that produced the sample; \code{get_rules()} shows both the
original requirement and the resolved subset, and \code{\link[=manifest]{manifest()}} logs the
resolution with the id count.
}
\examples{
frame <- create_sfw("balanced 990EZ filers")
frame <- add_rule(frame, type = "require", table = "P00",
                  present_in = "all", column = "RETURN_TYPE",
                  op = "in", values = "990EZ", holds = "every")
get_rules(frame)
\dontrun{
# Reads EIN2, TAX_YEAR, and RETURN_TYPE only.
frame <- resolve_frame(frame, years = 2019:2021,
                       source = data_source(format = "parquet"))
get_rules(frame)
}
}
\seealso{
\code{\link[=add_rule]{add_rule()}}, \code{\link[=panelize]{panelize()}}, \code{\link[=manifest]{manifest()}}
}

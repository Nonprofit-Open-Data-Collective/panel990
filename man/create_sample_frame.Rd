% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample-frame.R
\name{create_sample_frame}
\alias{create_sample_frame}
\title{Create a sample frame}
\usage{
create_sample_frame(
  name,
  id_col = "EIN2",
  time_col = "TAX_YEAR",
  source = NA_character_,
  ...
)
}
\arguments{
\item{name}{Required project/panel name (used for logging and identification).}

\item{id_col}{Organization identifier column. Default \code{"EIN2"}.}

\item{time_col}{Panel time column. Default \code{"TAX_YEAR"}.}

\item{source}{Optional data-source description recorded in metadata.}

\item{...}{Convenience filters, e.g. \code{state = "GA"}, \code{years = 2020:2022}.}
}
\value{
A \code{sample_frame} object.
}
\description{
A sample frame is a reusable, free-standing specification of which rows and
columns belong in a research dataset, plus derived labels and operation
policies. It stores rules as data so they can be applied, checked, logged,
and serialized consistently across a workflow.
}
\details{
Named arguments in \code{...} are convenience filters: recognized aliases
(\code{years}, \code{formtype}, \code{state}, \code{county}, \code{msa}, \code{ntee}, \code{ntee_industry},
\code{subsection}, \code{eins}) map to their columns; any other name is treated as a
literal column with an \verb{in} filter.
}
\seealso{
\code{\link[=add_filter]{add_filter()}}, \code{\link[=keep_cols]{keep_cols()}}, \code{\link[=apply_sfw]{apply_sfw()}}, \code{\link[=conform]{conform()}}.
}

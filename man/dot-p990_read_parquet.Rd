% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format.R
\name{.p990_read_parquet}
\alias{.p990_read_parquet}
\title{Read a parquet file, projecting columns and pushing row filters down}
\usage{
.p990_read_parquet(
  path,
  columns = NULL,
  filters = NULL,
  engine = .p990_parquet_engine(),
  con = NULL
)
}
\arguments{
\item{path}{Local path or URL.}

\item{columns}{Fields to retain; \code{NULL} reads every column.}

\item{filters}{Named list of accepted values, such as \code{list(EIN2 = eins)}.}

\item{engine}{\code{"duckdb"} or \code{"arrow"}.}

\item{con}{Optional open DuckDB connection to reuse across a read loop.}
}
\value{
A data frame of character columns, uncast. Callers apply
\code{\link[=.p990_coerce]{.p990_coerce()}}.
}
\description{
Column projection and an \code{IN} filter are applied during the scan, so a
selective read touches only the column chunks and row groups it needs. The
efile files are sorted by \code{EIN2}, which is what makes an entity restriction
prune row groups rather than scan them.
}
\keyword{internal}

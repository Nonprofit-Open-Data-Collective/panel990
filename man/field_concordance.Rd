% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{field_concordance}
\alias{field_concordance}
\title{Field-scope and normalization concordance for IRS 990 efile variables}
\format{
A data frame with one row per \code{variable_name} and the columns:
\describe{
\item{variable_name}{Research-database field name (matches ef2 table columns).}
\item{description}{Field description from the 990 forms.}
\item{variable_scope}{Form scope: \code{PC} (full 990 only), \code{EZ} (990EZ only),
\code{PZ} (both forms), \code{HD} (header), \code{SG} (signature block).}
\item{form_type}{Originating form of the mapped xpath.}
\item{data_type_simple}{Simplified R type: \code{numeric}, \code{checkbox}, \code{text}, \code{date}.}
\item{data_type_xsd}{XSD schema type (e.g. \code{USAmountType}), used to detect money fields.}
\item{money_field}{\code{TRUE} when a numeric field holds a US dollar amount.}
\item{blank_meaning}{How to read an in-scope blank: \code{implicit_zero}
(blank money amount is 0), \code{implicit_false} (blank checkbox is \code{FALSE}),
or \code{literal_missing} (text, dates, and non-money numerics such as counts,
ratios, and identifiers).}
\item{forms}{Applicable return forms as a \code{|}-separated string
(\code{"990"}, \code{"990EZ"}, \code{"990|990EZ"}, or \code{"*"}), derived from \code{variable_scope}.}
\item{rdb_table}{Primary ef2 table for the field.}
\item{rdb_tables_all}{All ef2 tables containing the field (\verb{;}-separated).}
\item{rdb_relationship}{Table cardinality: \code{ONE} (1x1) or \code{MANY} (1xm).}
\item{current_version}{\code{TRUE} when the field appears in a current XSD schema.}
\item{n_xpaths}{Number of source xpath rows collapsed into this variable.}
\item{scope_conflict, type_conflict}{\code{TRUE} when source rows disagreed on
scope or type and a value was chosen by the build rule.}
}
}
\source{
IRS Efile Master Concordance File, Nonprofit Open Data Collective /
National Center for Charitable Statistics, distributed under the Open Data
Commons Attribution License (ODC-By) v1.0.
\url{https://github.com/Nonprofit-Open-Data-Collective/irs-efile-master-concordance-file}
}
\usage{
field_concordance
}
\description{
One row per research-database variable produced by the ef2 processing of the
IRS 990 efile XML files. It records each field's form scope and the intended
interpretation of a blank source value, and is the basis for the built-in
concordance returned by \code{\link[=concordance]{concordance()}}.
}
\details{
Built from the IRS Efile Master Concordance File by
\code{data-raw/build-concordance.R}. Blank meanings are assigned by type: numeric
money fields (by XSD type) become \code{implicit_zero}; checkboxes become
\code{implicit_false}; text, dates, and non-money numerics become
\code{literal_missing}. Conflicting source metadata is resolved by preferring the
current schema version, then the most frequent value.
}
\seealso{
\code{\link[=concordance]{concordance()}}, \code{\link[=fields_in_scope]{fields_in_scope()}}
}
\keyword{datasets}

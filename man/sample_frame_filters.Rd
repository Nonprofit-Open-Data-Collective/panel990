% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample-frame.R
\name{sample_frame_filters}
\alias{sample_frame_filters}
\alias{add_filter}
\alias{set_filter}
\alias{remove_filter}
\alias{clear_filters}
\alias{get_filters}
\alias{set_ids}
\title{Row filters for a sample frame}
\usage{
add_filter(
  sfw,
  column = NULL,
  op = "in",
  values = NULL,
  origin = NULL,
  label = NULL
)

set_filter(
  sfw,
  column = NULL,
  op = "in",
  values = NULL,
  origin = NULL,
  label = NULL
)

remove_filter(sfw, column = NULL, label = NULL, index = NULL)

clear_filters(sfw)

get_filters(sfw)

set_ids(sfw, ids)
}
\arguments{
\item{sfw}{A \code{sample_frame}.}

\item{column}{Column or derived-attribute name to test (ignored for \code{expr}).}

\item{op}{One of \verb{in}, \code{not_in}, \code{==}, \code{!=}, \code{>}, \code{>=}, \code{<}, \code{<=}, \code{between},
\code{is_true}, \code{is_false}, \code{expr}.}

\item{values}{Operand(s): a vector for \verb{in}/\code{not_in}, a scalar for
comparisons, length-2 for \code{between}, an expression string for \code{expr}.}

\item{origin}{Optional pushdown hint (\code{"efile"}, \code{"bmf"}, \code{"derived"}); may be
\code{NULL} -- filters apply to any data frame that has the column.}

\item{label}{Optional human-readable label used in logs.}

\item{index}{Integer position(s) to drop (\code{remove_filter}).}

\item{ids}{Character vector of identifiers (\code{set_ids}).}
}
\value{
The updated \code{sample_frame}, or a data frame for \code{get_filters()}.
}
\description{
Add, replace, remove, list, or clear the row-filter specifications. A filter
is a record of \code{column}, \code{op}, and \code{values}; \code{add_filter()} appends,
\code{set_filter()} replaces any existing filter on the same column.
}

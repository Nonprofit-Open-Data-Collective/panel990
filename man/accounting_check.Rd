% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accounting.R
\name{accounting_check}
\alias{accounting_check}
\title{Check 990 rows against accounting identities}
\usage{
accounting_check(
  data,
  section = NULL,
  id = "EIN2",
  time = "TAX_YEAR",
  tol = 1,
  violations_only = TRUE
)
}
\arguments{
\item{data}{A data frame of 990 financial fields (e.g. a merged panel).}

\item{section}{Identity sections to check (\code{"revenue"}, \code{"expenses"},
\code{"balance_sheet"}). \code{NULL} (default) checks all.}

\item{id, time}{Identifier columns carried onto the report (if present).}

\item{tol}{Absolute tolerance for calling a residual a violation.}

\item{violations_only}{Return only the rows that violate an identity
(default \code{TRUE}); \code{FALSE} returns every evaluated identity.}
}
\value{
A data frame with one row per (record, identity): the id/time keys,
\code{identity}, \code{residual}, and \code{ok}.
}
\description{
Evaluates the bundled \link{accounting_identities} against each row of a panel and
reports the residual of every identity that can be evaluated (all of its
variables present as columns). A residual is \code{total - sum(parts)}; it should
be zero.
}
\seealso{
\code{\link[=reconcile]{reconcile()}}, \link{accounting_identities}.
}

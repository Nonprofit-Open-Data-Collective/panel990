% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve.R
\name{.p990_fetch}
\alias{.p990_fetch}
\title{Fetch one remote or local resource, retrying on failure}
\usage{
.p990_fetch(
  url,
  destination,
  timeout = 1800,
  retry_max = 3L,
  expected_bytes = NA_real_,
  label = basename(destination),
  overwrite = FALSE,
  backoff = 5,
  verbose = TRUE
)
}
\arguments{
\item{url}{Remote URL or local file path.}

\item{destination}{Local destination path.}

\item{timeout}{Per-attempt timeout in seconds.}

\item{retry_max}{Maximum attempts.}

\item{expected_bytes}{Optional expected size used as an integrity check.}

\item{label}{Human-readable name used in progress messages.}

\item{overwrite}{Re-fetch even when the destination already exists.}

\item{backoff}{Seconds before the second attempt; doubles thereafter.}

\item{verbose}{Print START/OK/FAIL messages.}
}
\value{
A list with \code{status}, \code{attempts}, \code{bytes}, \code{seconds}, and \code{error}.
}
\description{
Raises the download timeout for the duration of the call (the R default of
60 seconds is far too short for multi-GB sources), retries with exponential
backoff, deletes partial files between attempts, and verifies the byte count
against \code{expected_bytes} when a published manifest supplies one.
}
\keyword{internal}

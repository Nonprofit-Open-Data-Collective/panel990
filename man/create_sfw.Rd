% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample-frame.R
\name{create_sfw}
\alias{create_sfw}
\title{Create a sample frame}
\usage{
create_sfw(
  name,
  entity = "EIN2",
  time = "TAX_YEAR",
  record = NULL,
  source = NA_character_,
  ...
)
}
\arguments{
\item{name}{Project/panel name (used for identification and logging).}

\item{entity}{Entity (organization) key column. Default \code{"EIN2"}. \code{NULL} to
register none.}

\item{time}{Time key column. Default \code{"TAX_YEAR"}. \code{NULL} to register none.}

\item{record}{Optional filing-level (\code{unique_record}) key column, e.g.
\code{"OBJECTID"}.}

\item{source}{Optional data-source description stored in metadata.}

\item{...}{Convenience filters lowered into filter rules.}
}
\value{
An object of class \code{sfw}.
}
\description{
A sample frame (\code{sfw}) is a free-standing registry of keys and typed rules
that specifies which rows and columns belong in a dataset. It applies to, and
validates against, any data frame. See \code{\link[=add_rule]{add_rule()}} for the rule types and
\code{\link[=apply_sfw]{apply_sfw()}} to realize them.
}
\details{
Default entity and time keys are registered from \code{entity}/\code{time}; add a
filing-level key with \code{\link[=add_key]{add_key()}}. Named \code{...} are convenience filters
(\code{state = "GA"}, \code{years = 2020:2022}).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-normalize.R
\name{panel_normalize}
\alias{panel_normalize}
\title{Normalize blank financial fields to zero, form-scoped}
\usage{
panel_normalize(
  data,
  fields = c("core", "all"),
  form = "RETURN_TYPE",
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame of 990 fields, or a \link[=as_panel]{panel}.}

\item{fields}{\code{"core"} (default) or \code{"all"} (see \code{\link[=financial_fields]{financial_fields()}}).}

\item{form}{Name of the return-type column. Default \code{"RETURN_TYPE"}.}

\item{verbose}{Print a summary.}
}
\value{
\code{data} with in-scope blank financial fields set to zero; a
\code{"normalize_audit"} attribute records what was changed.
}
\description{
Interprets blank core-990 financial cells as zero, respecting form scope and
protecting non-filer rows:
\itemize{
\item both-form (\code{PZ}) fields are zeroed for every filer;
\item full-990-only (\code{PC}) fields are zeroed only for non-990EZ filers (they
are out of scope on the 990EZ and left \code{NA});
\item rows with \strong{no} financial data at all (a non-filer / shell record)
are left untouched -- no fabricated zeros.
}
}
\details{
By default only the curated core financial statements are touched; text,
dates, counts, and checkboxes are left to \code{\link[=normalize]{normalize()}}. Given a
\link[=as_panel]{panel} the step is logged.
}
\seealso{
\code{\link[=financial_fields]{financial_fields()}}, \code{\link[=normalize]{normalize()}}, \code{\link[=panel_impute]{panel_impute()}}.
}

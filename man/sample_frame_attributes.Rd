% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample-frame.R
\name{sample_frame_attributes}
\alias{sample_frame_attributes}
\alias{attach_attribute}
\alias{drop_attribute}
\title{Derived attributes (class labels) on a sample frame}
\usage{
attach_attribute(sfw, name, map, ids = NULL)

drop_attribute(sfw, name)
}
\arguments{
\item{sfw}{A \code{sample_frame}.}

\item{name}{Attribute name.}

\item{map}{A named vector (names are ids) or a two-column data frame
(id, value).}

\item{ids}{Optional ids when \code{map} is unnamed.}
}
\value{
The updated \code{sample_frame}.
}
\description{
Attach or drop per-organization labels that live in the frame rather than the
data. Once attached, an attribute can be filtered on exactly like a column.
\code{\link[=classify_panel]{classify_panel()}} runs \code{\link[=panel_describe]{panel_describe()}} and stores its labels.
}
